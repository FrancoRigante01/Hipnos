---
// Componente para mostrar la galería de cartas coleccionables
---

<div class="card-gallery-container">
  <div class="gallery-header">
    <h2 class="gallery-title">🎴 Cartas Coleccionables</h2>
    <p class="gallery-subtitle">Desbloqueá cartas al soñar con diferentes elementos</p>
  </div>

  <div class="progress-section">
    <div class="progress-info">
      <span class="progress-label">Progreso de Colección</span>
      <span class="progress-count" id="cardProgressText">0/0 (0%)</span>
    </div>
    <div class="progress-bar-container">
      <div class="progress-bar-fill" id="cardProgressFill"></div>
    </div>
  </div>

  <div class="cards-grid" id="cardGallery">
    <!-- Las cartas se renderizarán aquí dinámicamente -->
  </div>
</div>

<style>
  .card-gallery-container {
    padding: 2rem;
    background: rgba(255, 255, 255, 0.03);
    border-radius: 20px;
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    margin-bottom: 2rem;
  }

  .gallery-header {
    text-align: center;
    margin-bottom: 2rem;
  }

  .gallery-title {
    font-size: 2rem;
    font-weight: 300;
    color: #f1f5f9;
    margin: 0 0 0.5rem 0;
    letter-spacing: 1px;
    background: linear-gradient(90deg, #e9e6f6 0%, #a390ff 50%, #ffd700 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .gallery-subtitle {
    font-size: 1rem;
    color: #94a3b8;
    margin: 0;
    font-weight: 300;
    font-style: italic;
  }

  .progress-section {
    margin-bottom: 2rem;
    padding: 1.5rem;
    background: rgba(15, 23, 42, 0.4);
    border-radius: 12px;
    border: 1px solid rgba(148, 163, 184, 0.2);
  }

  .progress-info {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1rem;
  }

  .progress-label {
    color: #cbd5e1;
    font-size: 0.95rem;
    font-weight: 500;
  }

  .progress-count {
    color: #a390ff;
    font-size: 1.1rem;
    font-weight: 600;
  }

  .progress-bar-container {
    width: 100%;
    height: 12px;
    background: rgba(15, 23, 42, 0.5);
    border-radius: 6px;
    overflow: hidden;
    border: 1px solid rgba(148, 163, 184, 0.2);
  }

  .progress-bar-fill {
    height: 100%;
    background: linear-gradient(90deg, #6366f1, #8b5cf6, #a390ff);
    border-radius: 6px;
    width: 0%;
    transition: width 0.8s ease;
    box-shadow: 0 0 10px rgba(139, 92, 246, 0.5);
  }

  .cards-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 1.5rem;
  }

  /* Estilos para las cartas individuales */
  .collectible-card {
    border-radius: 16px;
    padding: 1.5rem;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
    min-height: 220px;
    display: flex;
    flex-direction: column;
  }

  .collectible-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    border-radius: 16px;
    padding: 2px;
    background: linear-gradient(135deg, transparent, rgba(255,255,255,0.1), transparent);
    -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
    -webkit-mask-composite: xor;
    mask-composite: exclude;
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .collectible-card:hover::before {
    opacity: 1;
  }

  /* Raridades */
  .collectible-card.rarity-común {
    background: linear-gradient(135deg, rgba(100, 116, 139, 0.3) 0%, rgba(71, 85, 105, 0.3) 100%);
    border: 1px solid rgba(148, 163, 184, 0.4);
  }

  .collectible-card.rarity-rara {
    background: linear-gradient(135deg, rgba(59, 130, 246, 0.3) 0%, rgba(37, 99, 235, 0.3) 100%);
    border: 1px solid rgba(96, 165, 250, 0.5);
  }

  .collectible-card.rarity-épica {
    background: linear-gradient(135deg, rgba(168, 85, 247, 0.3) 0%, rgba(147, 51, 234, 0.3) 100%);
    border: 1px solid rgba(192, 132, 252, 0.5);
  }

  .collectible-card.rarity-legendaria {
    background: linear-gradient(135deg, rgba(234, 179, 8, 0.3) 0%, rgba(202, 138, 4, 0.3) 100%);
    border: 1px solid rgba(250, 204, 21, 0.6);
    box-shadow: 0 0 20px rgba(234, 179, 8, 0.3);
  }

  /* Estado bloqueado */
  .collectible-card.locked {
    opacity: 0.6;
    filter: grayscale(80%);
  }

  .collectible-card.locked:hover {
    opacity: 0.75;
    transform: translateY(-3px);
  }

  /* Estado desbloqueado */
  .collectible-card.unlocked:hover {
    transform: translateY(-8px);
    box-shadow: 0 12px 30px rgba(139, 92, 246, 0.4);
  }

  .card-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    flex: 1;
  }

  .card-emoji {
    font-size: 4rem;
    margin-bottom: 1rem;
    filter: drop-shadow(0 0 10px rgba(167, 139, 250, 0.6));
  }

  .collectible-card.locked .card-emoji {
    filter: grayscale(100%) opacity(0.5);
  }

  .card-rarity-badge {
    display: inline-block;
    padding: 0.25rem 0.75rem;
    border-radius: 12px;
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 1px;
    margin-bottom: 0.75rem;
    background: rgba(0, 0, 0, 0.3);
    border: 1px solid rgba(255, 255, 255, 0.2);
  }

  .rarity-común .card-rarity-badge {
    color: #cbd5e1;
  }

  .rarity-rara .card-rarity-badge {
    color: #93c5fd;
  }

  .rarity-épica .card-rarity-badge {
    color: #c084fc;
  }

  .rarity-legendaria .card-rarity-badge {
    color: #fde047;
    text-shadow: 0 0 10px rgba(234, 179, 8, 0.8);
  }

  .card-title {
    font-size: 1.3rem;
    font-weight: 600;
    color: #f1f5f9;
    margin: 0 0 0.5rem 0;
    line-height: 1.3;
  }

  .collectible-card.locked .card-title {
    color: #94a3b8;
  }

  .card-description {
    font-size: 0.95rem;
    color: #cbd5e1;
    margin: 0 0 1rem 0;
    line-height: 1.5;
    flex: 1;
  }

  .collectible-card.locked .card-description {
    color: #64748b;
  }

  .card-hint {
    font-size: 0.85rem;
    color: #94a3b8;
    margin: 0;
    font-style: italic;
    padding: 0.75rem;
    background: rgba(15, 23, 42, 0.4);
    border-radius: 8px;
    border: 1px solid rgba(148, 163, 184, 0.2);
  }

  /* Animación de aparición de cartas */
  @keyframes cardAppear {
    from {
      opacity: 0;
      transform: translateY(20px) scale(0.9);
    }
    to {
      opacity: 1;
      transform: translateY(0) scale(1);
    }
  }

  .collectible-card {
    animation: cardAppear 0.5s ease backwards;
  }

  .collectible-card:nth-child(1) { animation-delay: 0.05s; }
  .collectible-card:nth-child(2) { animation-delay: 0.1s; }
  .collectible-card:nth-child(3) { animation-delay: 0.15s; }
  .collectible-card:nth-child(4) { animation-delay: 0.2s; }
  .collectible-card:nth-child(5) { animation-delay: 0.25s; }
  .collectible-card:nth-child(6) { animation-delay: 0.3s; }
  .collectible-card:nth-child(7) { animation-delay: 0.35s; }
  .collectible-card:nth-child(8) { animation-delay: 0.4s; }
  .collectible-card:nth-child(9) { animation-delay: 0.45s; }
  .collectible-card:nth-child(10) { animation-delay: 0.5s; }
  .collectible-card:nth-child(11) { animation-delay: 0.55s; }
  .collectible-card:nth-child(12) { animation-delay: 0.6s; }

  @media (max-width: 768px) {
    .card-gallery-container {
      padding: 1.5rem;
    }

    .gallery-title {
      font-size: 1.6rem;
    }

    .gallery-subtitle {
      font-size: 0.9rem;
    }

    .cards-grid {
      grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
      gap: 1rem;
    }

    .collectible-card {
      padding: 1.25rem;
      min-height: 200px;
    }

    .card-emoji {
      font-size: 3rem;
    }

    .card-title {
      font-size: 1.1rem;
    }

    .card-description {
      font-size: 0.85rem;
    }
  }
</style>

<!-- Estilos globales para la notificación de desbloqueo -->
<style is:global>
  .card-unlock-notification {
    position: fixed;
    top: 20px;
    right: 20px;
    z-index: 10000;
    background: linear-gradient(135deg, rgba(99, 102, 241, 0.95) 0%, rgba(139, 92, 246, 0.95) 100%);
    border: 2px solid rgba(167, 139, 250, 0.8);
    border-radius: 16px;
    padding: 1.5rem;
    box-shadow: 0 10px 40px rgba(139, 92, 246, 0.5);
    backdrop-filter: blur(10px);
    transform: translateX(400px);
    opacity: 0;
    transition: all 0.5s cubic-bezier(0.68, -0.55, 0.265, 1.55);
    max-width: 350px;
  }

  .card-unlock-notification.show {
    transform: translateX(0);
    opacity: 1;
  }

  .unlock-content {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .unlock-emoji {
    font-size: 3.5rem;
    animation: unlockBounce 0.8s ease;
    filter: drop-shadow(0 0 10px rgba(255, 255, 255, 0.8));
  }

  @keyframes unlockBounce {
    0%, 100% { transform: scale(1); }
    25% { transform: scale(1.3) rotate(-10deg); }
    50% { transform: scale(0.9) rotate(10deg); }
    75% { transform: scale(1.1) rotate(-5deg); }
  }

  .unlock-text {
    flex: 1;
  }

  .unlock-title {
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.9);
    font-weight: 500;
    margin-bottom: 0.25rem;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .unlock-name {
    font-size: 1.3rem;
    color: white;
    font-weight: 700;
    margin-bottom: 0.25rem;
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
  }

  .unlock-rarity {
    display: inline-block;
    font-size: 0.75rem;
    font-weight: 700;
    padding: 0.25rem 0.6rem;
    border-radius: 8px;
    letter-spacing: 0.5px;
  }

  .unlock-rarity.rarity-común {
    background: rgba(148, 163, 184, 0.3);
    color: #cbd5e1;
  }

  .unlock-rarity.rarity-rara {
    background: rgba(59, 130, 246, 0.3);
    color: #93c5fd;
  }

  .unlock-rarity.rarity-épica {
    background: rgba(168, 85, 247, 0.3);
    color: #c084fc;
  }

  .unlock-rarity.rarity-legendaria {
    background: rgba(234, 179, 8, 0.3);
    color: #fde047;
    text-shadow: 0 0 8px rgba(234, 179, 8, 0.8);
  }

  @media (max-width: 768px) {
    .card-unlock-notification {
      right: 10px;
      top: 10px;
      max-width: calc(100vw - 40px);
      padding: 1rem;
    }

    .unlock-emoji {
      font-size: 2.5rem;
    }

    .unlock-name {
      font-size: 1.1rem;
    }
  }
</style>
