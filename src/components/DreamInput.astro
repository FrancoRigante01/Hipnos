---
// Componente para ingresar el sueño mediante voz o texto
---

<div class="dream-input-container">
  <h2 class="dream-input-title">Relátame tu sueño, mortal</h2>
  <p class="dream-input-subtitle">Elige cómo compartir tus visiones nocturnas</p>
  
  <!-- Selector de modo -->
  <div class="mode-selector">
    <button class="mode-btn active" id="voiceModeBtn" data-mode="voice">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M12 1C10.34 1 9 2.34 9 4V12C9 13.66 10.34 15 12 15C13.66 15 15 13.66 15 12V4C15 2.34 13.66 1 12 1Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        <path d="M19 10V12C19 15.866 15.866 19 12 19C8.13401 19 5 15.866 5 12V10" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
      <span>Hablar</span>
    </button>
    <button class="mode-btn" id="textModeBtn" data-mode="text">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M17 3H5C3.89543 3 3 3.89543 3 5V19C3 20.1046 3.89543 21 5 21H19C20.1046 21 21 20.1046 21 19V7L17 3Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        <path d="M9 9H15M9 13H15M9 17H13" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
      <span>Escribir</span>
    </button>
  </div>

  <div class="input-wrapper">
    <!-- Botón de voz -->
    <button id="voiceBtn" class="voice-btn">
      <div class="mystical-circle">
        <div class="stars-container">
          <span class="star">✦</span>
          <span class="star">✧</span>
          <span class="star">✦</span>
          <span class="star">✧</span>
          <span class="star">✦</span>
          <span class="star">✧</span>
        </div>
      </div>
      <div class="mic-icon-wrapper">
        <svg class="mic-icon" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
          <path d="M12 1C10.34 1 9 2.34 9 4V12C9 13.66 10.34 15 12 15C13.66 15 15 13.66 15 12V4C15 2.34 13.66 1 12 1Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          <path d="M19 10V12C19 15.866 15.866 19 12 19C8.13401 19 5 15.866 5 12V10" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          <path d="M12 19V23" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          <path d="M8 23H16" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
        <div class="ethereal-glow"></div>
      </div>
      <span class="voice-btn-text" id="voiceBtnText">Narra tu sueño</span>
      <div class="runes-circle">
        <span class="rune">◐</span>
        <span class="rune">☽</span>
        <span class="rune">◑</span>
        <span class="rune">☾</span>
      </div>
    </button>

    <!-- Estado de escucha -->
    <div class="listening-indicator hidden" id="listeningIndicator">
      <div class="sound-wave">
        <span class="bar"></span>
        <span class="bar"></span>
        <span class="bar"></span>
        <span class="bar"></span>
        <span class="bar"></span>
      </div>
      <p class="listening-text">Escuchando...</p>
    </div>

    <!-- Modo escritura manual -->
    <div class="text-input-container hidden" id="textInputContainer">
      <textarea 
        id="dreamText" 
        class="dream-textarea" 
        placeholder="Soñé que..."
        rows="6"
      ></textarea>
    </div>

    <!-- Área de transcripción (modo voz) -->
    <div class="transcription-container hidden" id="transcriptionContainer">
      <div class="transcription-header">
        <span class="transcription-label">Tu relato</span>
        <button class="clear-transcription" id="clearTranscription" title="Borrar">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M18 6L6 18M6 6L18 18" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </button>
      </div>
      <div class="transcription-text" id="transcriptionText" contenteditable="true"></div>
    </div>
    
    <!-- Botón de interpretación -->
    <button id="interpretBtn" class="interpret-btn hidden">
      <span class="btn-text">Revelar significado</span>
      <span class="btn-icon">✨</span>
    </button>

    <!-- Mensaje de compatibilidad -->
    <div class="browser-warning hidden" id="browserWarning">
      <p>⚠️ Tu navegador no soporta reconocimiento de voz. Usa Chrome, Edge o Safari.</p>
    </div>
  </div>
</div>

<style>
  .dream-input-container {
    padding: 2rem;
    background: rgba(255, 255, 255, 0.03);
    border-radius: 20px;
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    margin-bottom: 2rem;
    transition: all 0.3s ease;
  }

  .dream-input-container:hover {
    background: rgba(255, 255, 255, 0.05);
    border-color: rgba(255, 255, 255, 0.2);
  }

  .dream-input-title {
    font-size: 1.8rem;
    font-weight: 300;
    color: #f1f5f9;
    margin: 0 0 0.5rem 0;
    letter-spacing: 0.5px;
  }

  .dream-input-subtitle {
    font-size: 0.95rem;
    color: #94a3b8;
    margin: 0 0 1.5rem 0;
    font-weight: 300;
  }

  .mode-selector {
    display: flex;
    gap: 0.75rem;
    justify-content: center;
    margin-bottom: 1.5rem;
    padding: 0.5rem;
    background: rgba(15, 23, 42, 0.3);
    border-radius: 12px;
    border: 1px solid rgba(148, 163, 184, 0.2);
  }

  .mode-btn {
    flex: 1;
    padding: 0.75rem 1.25rem;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(148, 163, 184, 0.2);
    border-radius: 8px;
    color: #94a3b8;
    font-size: 0.95rem;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    transition: all 0.3s ease;
  }

  .mode-btn svg {
    width: 18px;
    height: 18px;
  }

  .mode-btn:hover {
    background: rgba(255, 255, 255, 0.08);
    border-color: rgba(148, 163, 184, 0.3);
    color: #cbd5e1;
  }

  .mode-btn.active {
    background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
    border-color: rgba(139, 92, 246, 0.5);
    color: white;
    box-shadow: 0 4px 12px rgba(139, 92, 246, 0.3);
  }

  .input-wrapper {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    align-items: center;
  }

  .hidden {
    display: none !important;
  }

  /* Modo texto manual */
  .text-input-container {
    width: 100%;
    animation: fadeIn 0.4s ease;
  }

  .dream-textarea {
    width: 100%;
    padding: 1rem;
    background: rgba(15, 23, 42, 0.5);
    border: 1px solid rgba(148, 163, 184, 0.3);
    border-radius: 12px;
    color: #e2e8f0;
    font-size: 1rem;
    font-family: inherit;
    resize: vertical;
    min-height: 120px;
    transition: all 0.3s ease;
    line-height: 1.6;
  }

  .dream-textarea::placeholder {
    color: #64748b;
    font-style: italic;
  }

  .dream-textarea:focus {
    outline: none;
    border-color: rgba(167, 139, 250, 0.6);
    background: rgba(15, 23, 42, 0.7);
    box-shadow: 0 0 20px rgba(167, 139, 250, 0.15);
  }

  /* Botón de voz principal - Estilo místico */
  .voice-btn {
    width: 220px;
    height: 220px;
    border-radius: 50%;
    background: radial-gradient(circle at 30% 30%, rgba(139, 92, 246, 0.2), rgba(30, 27, 75, 0.9));
    border: 3px solid rgba(167, 139, 250, 0.4);
    cursor: pointer;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    transition: all 0.5s ease;
    position: relative;
    box-shadow: 
      0 0 30px rgba(139, 92, 246, 0.3),
      inset 0 0 30px rgba(139, 92, 246, 0.1);
    backdrop-filter: blur(10px);
  }

  .voice-btn::before {
    content: '';
    position: absolute;
    top: -3px;
    left: -3px;
    right: -3px;
    bottom: -3px;
    border-radius: 50%;
    background: linear-gradient(45deg, 
      rgba(167, 139, 250, 0.3), 
      rgba(139, 92, 246, 0.3),
      rgba(99, 102, 241, 0.3),
      rgba(139, 92, 246, 0.3),
      rgba(167, 139, 250, 0.3)
    );
    background-size: 300% 300%;
    animation: gradientRotate 6s ease infinite;
    z-index: -1;
    opacity: 0.6;
  }

  @keyframes gradientRotate {
    0%, 100% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
  }

  .voice-btn:hover {
    transform: scale(1.05);
    box-shadow: 
      0 0 50px rgba(139, 92, 246, 0.5),
      inset 0 0 40px rgba(139, 92, 246, 0.15);
    border-color: rgba(167, 139, 250, 0.6);
  }

  .voice-btn:active {
    transform: scale(0.98);
  }

  .voice-btn.listening {
    background: radial-gradient(circle at 30% 30%, rgba(239, 68, 68, 0.3), rgba(75, 27, 27, 0.9));
    border-color: rgba(248, 113, 113, 0.5);
    animation: mysticPulse 2s ease-in-out infinite;
  }

  @keyframes mysticPulse {
    0%, 100% {
      box-shadow: 
        0 0 30px rgba(239, 68, 68, 0.4),
        inset 0 0 30px rgba(239, 68, 68, 0.2);
    }
    50% {
      box-shadow: 
        0 0 60px rgba(239, 68, 68, 0.6),
        inset 0 0 50px rgba(239, 68, 68, 0.3);
    }
  }

  /* Círculo místico de fondo */
  .mystical-circle {
    position: absolute;
    width: 100%;
    height: 100%;
    border-radius: 50%;
    pointer-events: none;
  }

  /* Estrellas */
  .stars-container {
    position: absolute;
    width: 100%;
    height: 100%;
    animation: rotate 20s linear infinite;
  }

  .star {
    position: absolute;
    color: rgba(244, 244, 245, 0.6);
    font-size: 12px;
    animation: twinkle 3s ease-in-out infinite;
  }

  .star:nth-child(1) { top: 20%; left: 25%; animation-delay: 0s; }
  .star:nth-child(2) { top: 30%; right: 20%; animation-delay: 0.5s; }
  .star:nth-child(3) { bottom: 25%; left: 20%; animation-delay: 1s; }
  .star:nth-child(4) { bottom: 30%; right: 25%; animation-delay: 1.5s; }
  .star:nth-child(5) { top: 50%; left: 10%; animation-delay: 2s; }
  .star:nth-child(6) { top: 50%; right: 10%; animation-delay: 2.5s; }

  @keyframes rotate {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  @keyframes twinkle {
    0%, 100% { opacity: 0.3; transform: scale(1); }
    50% { opacity: 1; transform: scale(1.3); }
  }

  /* Runas circulares */
  .runes-circle {
    position: absolute;
    width: 90%;
    height: 90%;
    border-radius: 50%;
    animation: rotate 30s linear infinite reverse;
    pointer-events: none;
  }

  .rune {
    position: absolute;
    color: rgba(167, 139, 250, 0.5);
    font-size: 18px;
    animation: runePulse 4s ease-in-out infinite;
  }

  .rune:nth-child(1) { top: 5%; left: 50%; transform: translateX(-50%); animation-delay: 0s; }
  .rune:nth-child(2) { top: 50%; left: 5%; transform: translateY(-50%); animation-delay: 1s; }
  .rune:nth-child(3) { bottom: 5%; left: 50%; transform: translateX(-50%); animation-delay: 2s; }
  .rune:nth-child(4) { top: 50%; right: 5%; transform: translateY(-50%); animation-delay: 3s; }

  @keyframes runePulse {
    0%, 100% { opacity: 0.3; }
    50% { opacity: 0.8; }
  }

  .mic-icon-wrapper {
    position: relative;
    width: 60px;
    height: 60px;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 2;
  }

  .mic-icon {
    width: 40px;
    height: 40px;
    color: rgba(244, 244, 245, 0.9);
    position: relative;
    z-index: 2;
    filter: drop-shadow(0 0 8px rgba(167, 139, 250, 0.6));
  }

  /* Brillo etéreo */
  .ethereal-glow {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 70px;
    height: 70px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(167, 139, 250, 0.3), transparent 70%);
    animation: etherealPulse 3s ease-in-out infinite;
  }

  @keyframes etherealPulse {
    0%, 100% { 
      transform: translate(-50%, -50%) scale(1);
      opacity: 0.5;
    }
    50% { 
      transform: translate(-50%, -50%) scale(1.3);
      opacity: 0.8;
    }
  }

  .voice-btn.listening .ethereal-glow {
    background: radial-gradient(circle, rgba(248, 113, 113, 0.4), transparent 70%);
  }

  .voice-btn-text {
    color: rgba(244, 244, 245, 0.9);
    font-size: 0.95rem;
    font-weight: 400;
    letter-spacing: 1px;
    text-shadow: 0 0 10px rgba(167, 139, 250, 0.5);
    z-index: 2;
    font-family: 'Cinzel', serif;
  }

  /* Indicador de escucha */
  .listening-indicator {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }

  .sound-wave {
    display: flex;
    align-items: center;
    gap: 4px;
    height: 40px;
  }

  .sound-wave .bar {
    width: 4px;
    height: 100%;
    background: linear-gradient(135deg, #6366f1, #8b5cf6);
    border-radius: 2px;
    animation: soundWave 1s ease-in-out infinite;
  }

  .sound-wave .bar:nth-child(1) { animation-delay: 0s; }
  .sound-wave .bar:nth-child(2) { animation-delay: 0.1s; }
  .sound-wave .bar:nth-child(3) { animation-delay: 0.2s; }
  .sound-wave .bar:nth-child(4) { animation-delay: 0.3s; }
  .sound-wave .bar:nth-child(5) { animation-delay: 0.4s; }

  @keyframes soundWave {
    0%, 100% { height: 20%; }
    50% { height: 100%; }
  }

  .listening-text {
    color: #e2e8f0;
    font-size: 1rem;
    font-weight: 300;
    margin: 0;
  }

  /* Contenedor de transcripción */
  .transcription-container {
    width: 100%;
    background: rgba(15, 23, 42, 0.5);
    border: 1px solid rgba(148, 163, 184, 0.3);
    border-radius: 12px;
    padding: 1rem;
    transition: all 0.3s ease;
    animation: fadeIn 0.4s ease;
  }

  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(-10px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .transcription-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 0.75rem;
    padding-bottom: 0.5rem;
    border-bottom: 1px solid rgba(148, 163, 184, 0.2);
  }

  .transcription-label {
    color: #94a3b8;
    font-size: 0.85rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .clear-transcription {
    background: none;
    border: none;
    color: #94a3b8;
    cursor: pointer;
    padding: 0.25rem;
    transition: all 0.2s ease;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .clear-transcription:hover {
    color: #ef4444;
    transform: scale(1.1);
  }

  .transcription-text {
    color: #e2e8f0;
    font-size: 1rem;
    line-height: 1.6;
    min-height: 60px;
    max-height: 200px;
    overflow-y: auto;
    outline: none;
    padding: 0.5rem;
    border-radius: 4px;
    transition: background 0.2s ease;
  }

  .transcription-text:focus {
    background: rgba(15, 23, 42, 0.3);
  }

  .transcription-text:empty::before {
    content: 'Espero tus palabras...';
    color: #64748b;
    font-style: italic;
  }

  /* Advertencia de navegador */
  .browser-warning {
    background: rgba(239, 68, 68, 0.1);
    border: 1px solid rgba(239, 68, 68, 0.3);
    border-radius: 8px;
    padding: 1rem;
    width: 100%;
  }

  .browser-warning p {
    color: #fca5a5;
    font-size: 0.9rem;
    margin: 0;
    text-align: center;
  }

  /* Botón de interpretación */
  .interpret-btn {
    padding: 1rem 2rem;
    background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
    border: none;
    border-radius: 12px;
    color: white;
    font-size: 1.1rem;
    font-weight: 400;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
    letter-spacing: 0.3px;
    width: 100%;
  }

  .interpret-btn::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
    transition: left 0.5s ease;
  }

  .interpret-btn:hover::before {
    left: 100%;
  }

  .interpret-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(139, 92, 246, 0.4);
  }

  .interpret-btn:active {
    transform: translateY(0);
  }

  .btn-icon {
    font-size: 1.2rem;
    animation: sparkle 2s ease-in-out infinite;
  }

  @keyframes sparkle {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.7; transform: scale(1.1); }
  }

  @media (max-width: 768px) {
    .dream-input-container {
      padding: 1.5rem;
    }

    .dream-input-title {
      font-size: 1.5rem;
    }

    .mode-selector {
      flex-direction: column;
      gap: 0.5rem;
    }

    .mode-btn {
      font-size: 0.9rem;
      padding: 0.625rem 1rem;
    }

    .voice-btn {
      width: 180px;
      height: 180px;
    }

    .star {
      font-size: 10px;
    }

    .rune {
      font-size: 14px;
    }

    .mic-icon {
      width: 32px;
      height: 32px;
    }

    .voice-btn-text {
      font-size: 0.85rem;
    }

    .interpret-btn {
      font-size: 1rem;
      padding: 0.875rem 1.5rem;
    }
  }
</style>
