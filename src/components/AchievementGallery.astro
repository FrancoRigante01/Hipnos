---
// Componente para mostrar los logros desbloqueados y el progreso
---

<div class="achievement-gallery-container">
  <div class="gallery-header">
    <h2 class="gallery-title">🏆 Logros Oníricos</h2>
    <p class="gallery-subtitle">Desbloqueá logros al explorar diferentes temas en tus sueños</p>
  </div>

  <div class="progress-section">
    <div class="progress-info">
      <span class="progress-label">Progreso de Logros</span>
      <span class="progress-count" id="achievementProgressText">0/0 (0%)</span>
    </div>
    <div class="progress-bar-container">
      <div class="progress-bar-fill" id="achievementProgressFill"></div>
    </div>
  </div>

  <div class="achievements-list" id="achievementGallery">
    <!-- Los logros se renderizarán aquí dinámicamente -->
  </div>
</div>

<style>
  .achievement-gallery-container {
    padding: 2rem;
    background: rgba(255, 255, 255, 0.03);
    border-radius: 20px;
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    margin-bottom: 2rem;
  }

  .gallery-header {
    text-align: center;
    margin-bottom: 2rem;
  }

  .gallery-title {
    font-size: 2rem;
    font-weight: 300;
    color: #f1f5f9;
    margin: 0 0 0.5rem 0;
    letter-spacing: 1px;
    background: linear-gradient(90deg, #e9e6f6 0%, #a390ff 50%, #ffd700 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .gallery-subtitle {
    font-size: 1rem;
    color: #94a3b8;
    margin: 0;
    font-weight: 300;
    font-style: italic;
  }

  .progress-section {
    margin-bottom: 2rem;
    padding: 1.5rem;
    background: rgba(15, 23, 42, 0.4);
    border-radius: 12px;
    border: 1px solid rgba(148, 163, 184, 0.2);
  }

  .progress-info {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1rem;
  }

  .progress-label {
    color: #cbd5e1;
    font-size: 0.95rem;
    font-weight: 500;
  }

  .progress-count {
    color: #a390ff;
    font-size: 1.1rem;
    font-weight: 600;
  }

  .progress-bar-container {
    width: 100%;
    height: 12px;
    background: rgba(15, 23, 42, 0.5);
    border-radius: 6px;
    overflow: hidden;
    border: 1px solid rgba(148, 163, 184, 0.2);
  }

  .progress-bar-fill {
    height: 100%;
    background: linear-gradient(90deg, #6366f1, #8b5cf6, #a390ff);
    border-radius: 6px;
    width: 0%;
    transition: width 0.8s ease;
    box-shadow: 0 0 10px rgba(139, 92, 246, 0.5);
  }

  .achievements-list {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin-bottom: 2rem;
  }

  /* Estilos para los logros individuales */
  .achievement-item {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    padding: 1.5rem 1.5rem 2rem 2.5rem;
    background: rgba(15, 23, 42, 0.4);
    border-left: 4px solid #64748b;
    border-radius: 12px;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .achievement-item::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    border-radius: 16px;
    padding: 2px;
    background: linear-gradient(135deg, transparent, rgba(255,255,255,0.1), transparent);
    -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
    -webkit-mask-composite: xor;
    mask-composite: exclude;
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .achievement-item:hover::before {
    opacity: 1;
  }

  /* Raridades */
  .achievement-item.rarity-común {
    border-left-color: rgba(148, 163, 184, 0.6);
  }

  .achievement-item.rarity-rara {
    border-left-color: rgba(96, 165, 250, 0.8);
  }

  .achievement-item.rarity-épica {
    border-left-color: rgba(192, 132, 252, 0.8);
  }

  .achievement-item.rarity-legendaria {
    border-left-color: rgba(250, 204, 21, 0.9);
    box-shadow: 0 0 20px rgba(234, 179, 8, 0.2);
  }

  /* Estado bloqueado */
  .achievement-item.locked {
    opacity: 0.5;
  }

  .achievement-item.locked:hover {
    opacity: 0.65;
  }

  /* Estado desbloqueado */
  .achievement-item.unlocked:hover {
    transform: translateX(5px);
    box-shadow: 0 4px 20px rgba(139, 92, 246, 0.3);
  }

  .achievement-symbol {
    font-size: 3rem;
    min-width: 60px;
    text-align: center;
    filter: drop-shadow(0 0 8px currentColor);
    margin-left: 1rem;
  }

  .achievement-item.locked .achievement-symbol {
    filter: grayscale(100%) opacity(0.4);
  }

  .achievement-inner {
    display: flex;
    align-items: center;
    gap: 2rem;
    flex: 1;
  }

  .achievement-info {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    padding-bottom: 0.5rem;
  }

  .achievement-rarity-badge {
    display: inline-block;
    padding: 0.25rem 0.75rem;
    border-radius: 12px;
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 1px;
    width: fit-content;
    background: rgba(0, 0, 0, 0.3);
    border: 1px solid rgba(255, 255, 255, 0.2);
  }

  .rarity-común .achievement-rarity-badge {
    color: #cbd5e1;
  }

  .rarity-rara .achievement-rarity-badge {
    color: #93c5fd;
  }

  .rarity-épica .achievement-rarity-badge {
    color: #c084fc;
  }

  .rarity-legendaria .achievement-rarity-badge {
    color: #fde047;
    text-shadow: 0 0 10px rgba(234, 179, 8, 0.8);
  }

  .achievement-title {
    font-size: 1.2rem;
    font-weight: 600;
    color: #f1f5f9;
    margin: 0;
    line-height: 1.3;
  }

  .achievement-item.locked .achievement-title {
    color: #94a3b8;
  }

  .achievement-description {
    font-size: 0.9rem;
    color: #cbd5e1;
    margin: 0;
    line-height: 1.5;
  }

  .achievement-item.locked .achievement-description {
    color: #64748b;
  }

  .achievement-hint {
    font-size: 0.8rem;
    color: #94a3b8;
    margin: 0.5rem 0 0 0;
    font-style: italic;
  }

  /* Animación de aparición de logros */
  @keyframes achievementAppear {
    from {
      opacity: 0;
      transform: translateX(-20px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  .achievement-item {
    animation: achievementAppear 0.4s ease backwards;
  }

  .achievement-item:nth-child(1) { animation-delay: 0.05s; }
  .achievement-item:nth-child(2) { animation-delay: 0.1s; }
  .achievement-item:nth-child(3) { animation-delay: 0.15s; }
  .achievement-item:nth-child(4) { animation-delay: 0.2s; }
  .achievement-item:nth-child(5) { animation-delay: 0.25s; }
  .achievement-item:nth-child(6) { animation-delay: 0.3s; }
  .achievement-item:nth-child(7) { animation-delay: 0.35s; }
  .achievement-item:nth-child(8) { animation-delay: 0.4s; }
  .achievement-item:nth-child(9) { animation-delay: 0.45s; }
  .achievement-item:nth-child(10) { animation-delay: 0.5s; }
  .achievement-item:nth-child(11) { animation-delay: 0.55s; }
  .achievement-item:nth-child(12) { animation-delay: 0.6s; }

  @media (max-width: 768px) {
    .achievement-gallery-container {
      padding: 1.5rem;
    }

    .gallery-title {
      font-size: 1.6rem;
    }

    .gallery-subtitle {
      font-size: 0.9rem;
    }

    .achievement-item {
      flex-direction: column;
      text-align: center;
      padding: 1.25rem;
    }

    .achievement-symbol {
      font-size: 2.5rem;
    }

    .achievement-info {
      align-items: center;
    }

    .achievement-title {
      font-size: 1.1rem;
    }

    .achievement-description {
      font-size: 0.85rem;
    }
  }
</style>


